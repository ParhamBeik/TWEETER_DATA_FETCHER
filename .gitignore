# Python
.venv/
__pycache__/
*.py[cod]
*$py.class
*.egg-info/

# IDE
.claude/
.github/
.mimocode/
.vscode/

# OS
.DS_Store

# Large tweet/history datasets
LEGACY/TWEETER DATA FETCHING 1.0/TWEETS/
LEGACY/TWEETER DATA FETCHING 1.0/VIRAL TWEETS/
LEGACY/TWEETER DATA FETCHING 2.0/TWEETS/
LEGACY/TWEETER DATA FETCHING 2.0/VIRAL TWEETS/
LEGACY/TWEETER DATA FETCHING 2.0/data/
LEGACY/TWEETER DATA FETCHING 2.0/logs/
LEGACY/TWEETER DATA FETCHING 3.0/ADVANCED SEARCH/data/
LEGACY/TWEETER DATA FETCHING 3.0/HISTORICAL DATA/data/
LEGACY/TWEETER DATA FETCHING 3.0/LIVE DATA/data/
LEGACY/TWEETER DATA FETCHING 3.0/LIVE DATA/logs/

# Runtime data (gitignored)
data/

# Output directories generated by diagnostic tools
twitter_fetcher/diagnostics/probe_runs/
twitter_fetcher/diagnostics/sniffer_runs/
twitter_fetcher/diagnostics/graphql_logs/

# Generated knowledge graph (regenerate with `graphify update .`)
graphify-out/

# Legacy capture archive (superseded by v4 sniffer)
graphql_logs/

# Credentials (v1-v3) — live session tokens; never commit.
LEGACY/TWEETER DATA FETCHING 1.0/config.json
LEGACY/TWEETER DATA FETCHING 2.0/config.json
LEGACY/TWEETER DATA FETCHING 3.0/ADVANCED SEARCH/config.json
LEGACY/TWEETER DATA FETCHING 3.0/HISTORICAL DATA/config.json
LEGACY/TWEETER DATA FETCHING 3.0/LIVE DATA/config.json

# V4 config files (contain secrets)
twitter_fetcher/config/config.json
twitter_fetcher/config/config.json.backup
twitter_fetcher/config/config.json.bak

# Generated repomix context dumps (large; embed captured secrets)
**/repomix-output.md

.env

# Ignore all markdown and reference files in LEGACY
LEGACY/**/MARKDOWN FILES/
LEGACY/**/REFRENCES FILES/
LEGACY/**/*.md
LEGACY/**/*.txt

# Legacy runtime junk (logs, backups) — keep .py source tracked as an archive
LEGACY/**/logs/
LEGACY/**/*.bak
*.bak

# Playwright test run artifacts
test-results/
